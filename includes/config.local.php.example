<?php
// Server-only overrides (NOT in git). Copy to config.local.php on Hostinger.
if (!defined('SITE_URL')) {
    define('SITE_URL', 'https://www.nectradigital.com');
}
// Optional Facebook Pixel ID (create in Meta Business Suite)
// if (!defined('NECTRA_FB_PIXEL_ID')) {
//     define('NECTRA_FB_PIXEL_ID', 'YOUR_PIXEL_ID');
// }
// Google Cloud Translation API key (Cloud Console → Translation API → Credentials)
// Enables /api/translate for dynamic text + server-side cache. Site-wide UI uses Google Translate widget without key.
// if (!defined('GOOGLE_TRANSLATE_API_KEY')) {
//     define('GOOGLE_TRANSLATE_API_KEY', 'YOUR_GOOGLE_CLOUD_API_KEY');
// }
// DNS (Hostinger/cPanel): add SPF TXT record, e.g. v=spf1 include:_spf.google.com ~all
// Hostinger CDN: do NOT enable "Allow only specific countries" (India-only blocks Bing/Yahoo/Google from US).
// hPanel → Websites → CDN → Traffic blocking → remove all country rules for search indexing.
